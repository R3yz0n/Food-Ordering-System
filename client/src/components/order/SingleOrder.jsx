import React, { useState } from "react";

const SingleOrder = () => {
  const [isOrder, setIsOrder] = useState(false);
  return (
    <section>
      {/* No orders */}
      <div></div>
    </section>
  );
};

export default SingleOrder;
